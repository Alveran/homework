ufg01.c: In function ‘main’:
aufg01.c:16:3: error: expected ‘;’ before ‘return’
   return 0;
      ^~~~~~

Dies ist die Meldung des gcc nach dem Versuch zu kompilieren. Das Programm konnte nicht kompiliert werden und liegt somit auch nicht in einem ausführbarem Format vor.
